# 需求规格:首批标注数据的转换、训练与质量验证(Validation Run v1)

> 本文档交给 AI 编码助手(opencode / codex / Claude Code)执行。
> 全部代码用 Python 3.10+,依赖写入 requirements.txt,每个脚本支持 `--help`。
> 目标读者注意:本任务的目的是**验证标注质量与打通训练管线**,不是追求模型精度。请勿在模型调参上花费精力。

---

## 背景(供 AI 理解上下文)

四川麻将识别项目,第一人称真实牌桌视角,**两阶段架构**:
- 检测器:**单类 `tile_face`**(只检测有花色的牌面;牌背不检测、当背景)
- 分类器:28 类(27 牌面 + unknown),本任务暂不训练分类器,只为它准备 crop 数据

我已用 X-AnyLabeling 精细标注了约 70 张真实牌局图(来自 2 个 B 站博主),标注为 **28 类细类**(w1~w9 / t1~t9 / b1~b9 / unknown,矩形框)。现在要用这批数据跑通"转换 → 训练 → 验证"全链路,**验证我的标注质量是否过关、管线是否可用**,据此决定是否放心批量标注剩余 8 个博主。

### 输入数据

- 标注目录:`D:\mahjong\data\labeled\`(下含图片 + 每张图同名的 X-AnyLabeling `.json`)
- 标注格式:X-AnyLabeling 默认 JSON(每图一个,含 `shapes` 列表,每个 shape 有 `label`、`points`(矩形为两个对角点或四点)、`shape_type`)
- 类别:28 类(见上),可能含极少 `unknown`;**可能存在早期误标的 `back`(牌背)标签,需丢弃**

### 类别定义(写入 `configs/classes.yaml`,后续统一引用)

```yaml
detection:
  0: tile_face            # 单类:所有牌面
classification:           # 28 类(供未来分类器与本次 crop 导出)
  - [w1,w2,w3,w4,w5,w6,w7,w8,w9]
  - [t1,t2,t3,t4,t5,t6,t7,t8,t9]
  - [b1,b2,b3,b4,b5,b6,b7,b8,b9]
  - [unknown]
discard_labels: [back, tile_back]   # 出现即丢弃,不进任何数据集
```

---

## 任务 1:标注转换 `scripts/convert_labels.py`

**做什么**:把 X-AnyLabeling 标注转成两份产物——单类检测训练集(YOLO 格式)+ 分类 crop 集(按类分文件夹)。

**功能需求**:
1. 递归扫描 `labeled/`,配对图片与同名 json;无 json 的图跳过并记入报告
2. 解析每个 shape:取矩形框(若是四点多边形,取其外接矩形 = min/max x,y);`label` 在 `discard_labels` 中的**直接丢弃**;`label` 不在 28 类内的(拼写错误等)**单独列入异常清单**,不静默丢弃
3. **检测集导出(YOLO 格式)**:所有保留框的类别一律映射为 `tile_face`(类别 id=0);按 YOLO txt 格式输出(归一化 cx,cy,w,h);划分 train/val(默认 85/15,`--val-ratio` 可调,固定 seed 保证可复现);生成 `data.yaml`(单类,绝对路径)
4. **分类 crop 导出**:按每个框的**原 28 类细类**裁切原图(外扩 8% 边距,保留原始分辨率),按类分文件夹存入 `output/cls_crops/{类名}/`,文件名编码来源(原图名+框序号+尺寸桶);`unknown` 也单独成文件夹
5. **转换报告 `convert_report.json` + 控制台摘要**:
   - 图片数、总框数、丢弃的 back 框数、异常 label 清单(图名+label+坐标)
   - 28 类各类别框数分布(柱状文字即可)
   - 框尺寸分布(按较短边分桶:<20px / 20~40px / >40px)
   - 警告:某类框数为 0、或某类 <5(数据过少)

**验收标准**:对 `labeled/` 运行后,YOLO 检测集与 crop 集都生成;异常 label 清单准确(我可据此回 X-AnyLabeling 修正);报告数字与实际文件数一致。

---

## 任务 2:标注可视化抽查 `scripts/visualize_labels.py`

**做什么**:把标注画回图上,供我人眼快速复核(训练前先肉眼确认转换没出错)。

**功能需求**:
1. 随机抽 N 张(默认 20)图,把框 + 类别文字画上去,输出到 `output/label_preview/`
2. 颜色按花色区分(w/t/b 三色 + unknown 灰),小框文字不重叠
3. 单独再抽:每个"易混淆类"(t4/t5/t6/t9/各万字)各 5 个 crop 拼成对照图,供我确认这些类没标反

**验收标准**:预览图清晰可读;我能在 5 分钟内确认转换无误、标注无明显系统性错误。

---

## 任务 3:第一版检测器训练 `scripts/train_v1.py`

**做什么**:用转换好的单类检测集训一个临时检测器。**目的是验证,不是追求精度**。

**功能需求**:
1. 基于 Ultralytics YOLO11n(轻量、快),单类,`model=yolo11n.pt` 起步
2. 默认参数:`epochs=60 imgsz=960 batch=自动 patience=20`,全部命令行可覆盖
3. 训练中记录 loss 曲线、val 的 mAP@0.5、val 召回;输出到独立实验目录 `runs/val_run_v1/`(含配置快照、权重、曲线 png)
4. **明确提示**:在脚本开头打印一句说明——"本次为验证性训练,数据量小(~70张),精度低属正常,重点看标注质量与管线连通性",避免我误判
5. 训完打印 best.pt 路径

**验收标准**:训练正常收敛(loss 下降);产出 best.pt 与训练曲线;GPU 利用率正常(若有 GPU)。

---

## 任务 4:验证报告 `scripts/validation_report.py`(本任务核心交付)

**做什么**:自动生成一份"标注质量与管线"体检报告,让我**照着结论判断是否合格**,而不是自己去翻数字。

**功能需求**:
1. **管线连通性检查**:逐项 PASS/FAIL —— 转换成功?训练完成?权重存在?评测跑通?(任一 FAIL 醒目标红)
2. **训练健康度**:loss 是否下降(给曲线 + 自动判断"是/否在学习");val mAP 趋势
3. **标注自洽性检查(最重要)**:用训出的 best.pt **回头预测训练图**,做两件事:
   - 把"预测 vs 我的标注"画在一起,挑差异最大的 20 张输出到 `output/check/`(我的框绿色、模型预测红色)
   - 统计模型"学得像不像":在训练图上,模型框与我标注框的 IoU≥0.5 匹配率(高=标注自洽,模型学到了规律)
4. **系统性错误探测**:虽然检测是单类(无类别错),但分析**漏检/误检的模式**——是否集中在某类原始牌(从 crop 反查)、某尺寸桶、某博主;若某类原始牌框系统性学不会,提示"该类标注可能有问题,建议复查"
5. **真实域起点摸底**:报告按尺寸分桶的召回率(<20/20-40/>40px),让我知道当前距工业级差在哪个区域(预期小牌召回低)
6. **最终结论段**:自动按下列规则输出"合格/需复查"判断:
   - 管线全 PASS + loss 下降 + 训练图 IoU 匹配率 ≥ 0.7 + 无某类系统性学不会 → **合格,可批量标注剩余博主**
   - 任一不满足 → 列出具体问题与建议动作
7. 汇总成单文件 HTML(无外网依赖),含所有图表与结论

**验收标准**:报告打开即可读;结论段逻辑正确(对一份正常数据给"合格",对故意注入系统性错标的数据能给"需复查");我无需看代码或原始数字即可决策。

---

## 全局技术约束

- Python 3.10+;依赖:ultralytics, torch, opencv-python, pyyaml, numpy, matplotlib, jinja2, tqdm
- 路径用 `configs/paths.yaml` 配置(labeled 目录、输出目录),不写死在代码里
- 所有产物放 `output/` 与 `runs/`;脚本可重复运行(幂等)
- 不引入数据库/Web 服务;全部本地 CLI
- 重点:代码清晰可改,不过度设计;转换与匹配逻辑(IoU、外接矩形、归一化)加 pytest 单测

## 执行顺序

任务 1(转换)→ 任务 2(我肉眼抽查预览图,确认转换无误)→ 任务 3(训练)→ 任务 4(看验证报告,做"合格/复查"决策)

## 我(人类)负责的部分

- 提供 `D:\mahjong\data\labeled\` 标注数据
- 肉眼复核任务 2 的预览图、任务 4 报告里的差异图
- 根据验证报告结论决定:批量标注剩余 8 个博主,或先回去修正标注规范
- 提供 GPU 环境(本地或云)运行训练

## 不要做的事(避免跑偏)

- 不要为提升这一版的精度而调参、加数据增广、换大模型——70 张数据精度低是正常的,本任务不追求精度
- 不要把这批数据当测试集对待(它现在全部用于训练验证;正式测试集等 10 博主数据齐了再单独规划)
- 不要自动"修复"异常 label,只报告,由我手动回 X-AnyLabeling 改
