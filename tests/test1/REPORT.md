# 事件检出：不训新模型的新思路与实验（2026-08-05）

针对 `mahjong-rt/docs/OPEN_PROBLEM_events.md`。所有实验只消费已有录制
（`recordings_full/clip01`）和视频像素，未训练/重跑任何模型。

## 0. 结论速览

1. **最重要的发现不是算法，是真值本身：14 条 GT 时间戳系统性滞后约 1 秒**
   （口述标注的反应延迟）。±0.5s 容差下，任何对准物理落牌时刻的检测器都
   天然不及格，而慢 1 秒的检测器反而"碰巧"命中。**先修真值，再谈算法。**
2. 检测器输出层面（框的新颖度/标签翻转/站点出生）的时机信号经机制检验后
   **仍是噪声**：表面显著的 p=0.009 被证明是"级联双发 + 高密度覆盖"的假象。
   文档第 5 节的判断成立，并补充了新的微观机制（落牌引发牌堆重分割级联）。
3. 不训新模型仍有三条没试过且值得走的路：**修真值后重测一切**、
   **音频撞击声**（需新采集，当前 clips 无音轨）、**序列约束解码（Viterbi）**。

## 1. GT 时间戳滞后 ~1s —— 已修正（2026-08-05）

* 全部 14 条已按落桌帧重新锚定（`frames/fix/*.jpg` 条带，0.2s 间隔，精度 ±0.2s），
  原口述时刻备份在 `output/video_testset_pilot/events_gt.oral_t.json`。
  实测滞后 0.6~1.5s，均值 ~0.95s。`check_events_gt.py` 通过。
* **修正后重测（±0.5s，4000 次随机基线）：**

| 检测器 | 检出 | 命中 | 随机期望 | p | 结论 |
|---|---|---|---|---|---|
| 官方基线(game_events) | 13 | **4** | 5.0 | 0.86 | **低于随机**（原口径 +2.0 全是假象） |
| 实验A 框级新颖度 | 6 | 3 | 2.6 | 0.53 | 噪声 |
| 实验C2 站点出生(级联合并) | 15 | 7 | 5.6 | 0.27 | 噪声 |
| 官方基线+真值再平移±1.5s | 13 | ≤6 | ~4.8 | ≥0.26 | 任何滞后下都无信号 |

* 原口径下基线的"超出随机 +2.0"现在被直接证伪：它完全来自"标注滞后 1s ×
  基线自身检出延迟 ~1s"的对冲。文档"把它当成 0"的判断由推断变成实测。
* **但注意：修正真值没有救活任何检测器。** 对准物理落桌时刻后，所有基于
  检测器输出的时机信号依然是噪声——问题真的在观测量，不在真值。

## 2. 实验数字（全部，含失败）

评测口径与 `eval_game_events.py` 一致：±0.5s 贪心匹配 + 4000 次随机撒点基线。

| 实验 | 观测量 | 检出 | 命中(raw真值) | 随机期望 | p | 备注 |
|---|---|---|---|---|---|---|
| A | 框级"新物体"(IoU新颖度+持续性) | 6 | 3 | 2.6 | 0.52 | 失败：落牌框与既有框 IoU≈1.0 |
| A-diag | 诊断 | — | — | — | — | 真值落牌点 9/14 既有框 IoU≥0.98 |
| B | 像素"遗留物体"(GMC对齐+持续变化) | — | — | — | — | 曲线被手部淹没；用于测滞后 |
| C | 世界锚定站点标签翻转/出生 | 27 | 12 | 8.4 | 0.021 | 表面显著，实为级联双发 |
| C2 | +出生-死亡守恒过滤 | 20 | 11 | 6.9 | 0.009 | 同上假象 |
| C2合并 | 级联合并为单事件(1.2s NMS) | 15 | 7 | 5.6 | 0.26 | **合并即崩塌 → 证伪 C/C2** |
| D | 官方基线对平移真值 | 13 | 7→5 | 5.0 | 0.14→0.63 | 基线命中在标注时刻之后 |

机制发现（新）：落牌砸在牌堆上 → 检测器对邻域**重分割** → 一个落点产生
2~3 个站点事件（出生/翻转/改名），横跨约 1 秒，0.6s NMS 恰好放过去两条。
配合 ~1s 的标注滞后，第二条恰好落进 ±0.5s 窗口——这就是 p=0.009 的来源。
**这是文档陷阱②（扫参/密度假象）的第五个实例。**

命名子实验（唯一有正面迹象的）：落牌后**跳过前 0.5s**（翻滚+手指遮挡），
对 [0.5s, 1.5s] 窗口内重叠检测的 27 类后验取平均，命中事件中牌认对
4/7=57%（基线是 2/13）。样本太小不能下结论，但方向合理，值得并入后续方案。

## 2.5 实验E：摸牌手势轨迹（用户核心思路的决定性检验，2026-08-05）

用户的思路：用手势动作轨迹抓"摸牌"动作。分两步验证：

1. **分母测试通过**："手进牌墙区驻留≥3帧后离开"的签名在 30s 里只触发 11 次
   （真实摸牌 ~13 次），不像已死方案那样动辄 60-100 次。这是第一个过闸的手部信号。
2. **精度测试失败**：为 13 次摸牌帧级锚定了真值（`frames/draw/` 13 张全帧条带，
   锚定结果 `draw_gt.json`）。11 个候选对真值 **±0.5s 命中 0/13**；
   放宽到 ±1.0s 命中 4/13，仍低于随机期望 ~6.7。**不优于随机，此路在当前
   数据形态下也不通。**

死因（对后续有指导意义）：
* **me 的摸牌在近端牌墙，画面外** —— 2/13 连人眼都锚定不了。第一人称视角
  天然丢失自家摸牌动作。
* 对家的手常驻画面顶部"牌墙区"，区域型签名分不清"手在本垒"和"手在摸牌"。
* 抓取驻留只有 2-3 帧，而 MediaPipe 手心轨迹平均 9 帧断一次（887 帧 102 条
  轨迹）——**轨迹太碎，任何"轨迹形状"分析都建在流沙上**。
* 但条带同时证明：摸牌动作在原始像素里人眼清晰可辨（去-抓-回完整可见），
  信息在视频里，是 MediaPipe 点轨迹这个表示太贫瘠。要做手势路线，需要比
  点轨迹更富的表示（光流/手部 crop 序列），那已接近"训新模型"的范畴。

## 3. 不训新模型的思路（按建议优先级）

1. **修正真值后重测全部基线**（零模型成本，最高优先）。
   用条带工具把 14 个时刻锚到"牌离手落桌"那一帧。顺手把 clip02/03 也标了
   （文档第 6.4 节本就要求有碰的片段）。
2. **音频撞击声**（需新采集，当前 clips 无音轨——已验证）。
   牌落桌是尖锐脉冲，谱通量起始检测即可，无需训练；与视觉互为独立证据，
   融合后时机问题可能直接消失。AI 眼镜有麦克风，采集端把音频打开即可。
3. **序列约束解码**（文档方向 3 的具体化）：以 ~2.1s 周期、严格逆时针轮转、
   每种 4 张、牌池只增不减为先验，把 C2 的候选爆发点（含噪声）当观测做
   Viterbi/指派解码。单点观测烂没关系，序列先验能兜住。
   **但必须用修正后的真值验证，否则又是在拟合标注噪声。**
4. **"先降后升"偶合模式**（未测）：放置手在落点前会短暂遮挡牌堆
   （river 检测数骤降），随后净 +1。单独的出生信号已证是噪声，
   但"遮挡谷 → 出生峰"的时间偶合还没测过。
5. 碰牌先行（文档方向 4，不变）：跨帧确认后打开 `detect_melds`。

## 4. 产物清单（本目录）

| 文件 | 内容 |
|---|---|
| `exp_a_detect_novelty.py` / `exp_a_result.json` | 实验A：框级新颖度+后验平均命名 |
| `exp_a_diag.py` / `exp_a_diag.json` | 实验A诊断：真值落牌点的既有框 IoU |
| `exp_dump_frames.py` / `exp_dump_frames2.py` | 帧条带导出工具（全图/牌池放大） |
| `frames/*.jpg` | 6 张帧条带（3 全图 + 3 牌池放大），GT滞后证据 |
| `exp_b_pixel_persistence.py` / `exp_b_curves.npz` / `exp_b_curve.png` / `exp_b_pool_mask.png` | 实验B：像素遗留变化 |
| `exp_c_label_flip.py` / `exp_c_result.json` | 实验C：标签翻转 |
| `exp_c2_count_conserved.py` / `exp_c2_result_*.json` | 实验C2：守恒过滤+级联合并对照 |
| `exp_d_baseline_vs_lag.py` / `exp_d_result.json` | 实验D：官方基线 vs 平移真值 |
