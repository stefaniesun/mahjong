# 待解问题:麻将牌"对家区域"识别卡在 71%

> 给接手这个问题的人。数据、评测协议、已试过的方案与结果都在下面,可以直接开工。
> 路径以仓库根 `D:/mahjong/` 为基准。

---

## 1. 问题是什么

第一人称视角的麻将识别系统,已经能框出每张牌并认出是几万几条。**剩下的问题是:判断每张牌属于谁。**

五个区域:`my_hand`(我的手牌)/ `seat_left`(上家)/ `seat_across`(对家)/
`seat_right`(下家)/ `river`(中间的公共牌池)。

当前逐区域召回:

| 区域 | 召回 | 标注样本数 |
|---|---|---|
| my_hand | **100.0%** | 265 |
| river | 97.2% | 472 |
| seat_right | 97.8% | 46 |
| seat_left | 85.7% | 49 |
| **seat_across** | **71.0%** ← 待解 | **62** |

整体 94.5%(交叉验证)。**对家是唯一没有随迭代改善的区域**,四轮优化里一直卡在 71.0%。

### 为什么难

对家的牌与牌池在**三个维度上都重叠**:

* **位置**:对家 nx 中位 0.60,牌池 0.52,横向范围几乎完全重合(上家 0.23、下家 0.86
  在牌池范围之外,所以它们好分)
* **密度**:牌池是最密集的(弃牌堆在中间),对家次之,两者分布交叠
* **聚簇**:单链聚类会把对家的牌与牌池**串成一簇**(它们中间没有空隙)

现有的三个信号都分不开它。**需要一个新信号。**

---

## 2. 数据在哪

### 标注真值

`output/zone_annotation/zone_labels_with_class.json` — 20 张图、899 个框,人工标注。

```jsonc
[{
  "image":  "z01.jpg",                      // 对应 images/ 下的文件
  "source": "bili_1513200642__BV1zw...json", // 原始标注文件名(可追溯)
  "w": 1280, "h": 720,
  "boxes":  [[371.3, 540.0, 82.3, 92.8], ...],  // xywh 像素,GT 框(非检测器输出)
  "zones":  ["my_hand", "river", ...],           // 人工标注的区域 = 真值
  "cls":    ["b6", "t3", ...],                   // 牌的类别,w=万 t=条 b=筒
  "heuristic": ["river", ...]                    // 早期启发式的预测,可忽略
}]
```

* **`boxes` 是人工标注的真值框**,不含检测误差 —— 这个问题是纯粹的"给定框判区域"。
* **`cls` 是牌的类别**,目前的算法完全没用上。碰杠是 3~4 张相同的牌,这可能是个突破口
  (但 20 张图里只找到 5 组同类连排,样本太少)。

### 图片

`output/zone_annotation/images/z01.jpg` ~ `z20.jpg`,1280×720。

### 标注工具(要扩样本时用)

`output/zone_annotation/annotate.html` — 浏览器直接打开,拖框选一片按数字键批量改。
框已画好,只改标签。20 张图约 15 分钟。

### 标注口径(重要)

标注者的规则:**摆在某个人面前的牌算那家的**(包括定缺牌和碰杠),中间散乱那堆算牌池。
所以"某人面前"与"中间"的边界带有主观性,存在少量歧义样本。

---

## 3. 评测协议(务必照做,否则数字不可比)

**必须用图级 5 折交叉验证**,不能按框随机划分 —— 同一张图的牌高度相关,按框切会严重高估。

而且**阈值/参数必须在每折的训练图上重新拟合**,不能用全量数据拟合出的参数去报告交叉验证成绩。

这个项目在这上面栽过:早期报告过 92.5%,那是在同一批数据上网格搜索、再用同一批评测的
**训练集成绩**;同一个方法的诚实交叉验证成绩是 88.9%,虚高了 3.6 个点。

可直接运行的评测框架:

```python
import json, numpy as np, itertools

data = json.load(open('output/zone_annotation/zone_labels_with_class.json', encoding='utf-8'))

def cross_validate(predict_fn, grid, folds=5, seed=0):
    """predict_fn(sample, params) -> list[str],每个框一个区域标签"""
    rng = np.random.RandomState(seed)
    order = rng.permutation(len(data))
    parts = [order[i::folds] for i in range(folds)]

    def score(idxs, p):
        ok = n = 0
        for i in idxs:
            x = data[i]
            z = predict_fn(x, p)
            ok += sum(1 for a, b in zip(z, x['zones']) if a == b)
            n += len(z)
        return ok / max(n, 1)

    accs = []
    for part in parts:
        held = set(part.tolist())
        train = [i for i in range(len(data)) if i not in held]
        best = max(grid, key=lambda p: score(train, p))   # 只在训练折上选参数
        accs.append(score(part.tolist(), best))           # 在没见过的图上评测
    return float(np.mean(accs)), accs
```

自检:把下面两个基线跑一遍,数字应该对得上,否则说明数据或协议接错了。

```python
def all_river(x, p):
    return ['river'] * len(x['boxes'])

print(cross_validate(all_river, [None])[0])   # 应为 0.5235
```

报告时请给出:**整体交叉验证准确率 + 逐区域召回**(尤其 seat_across)。

参考线:

| | CV 准确率 |
|---|---|
| 全判 river | 52.35% |
| 当前 v8 | **94.53%** |

注意 v8 用固定参数(全量拟合)在这个框架里会跑出 94.99% —— 略高于 94.53%,因为参数见过
全部数据。报告新方法时请让参数在每折内重新拟合,数字才可比。

---

## 4. 已经试过的(别重复)

当前实现:`mahjong-rt/mahjong_rt/zones.py`,参数在 `mahjong-rt/configs/pipeline.yaml`。

| 版本 | 方法 | CV 准确率 | seat_across |
|---|---|---|---|
| v1~v3 | 手调阈值(位置为主) | 最好 39.7%(全量) | — |
| — | 基线:全部判 river | **52.5%** | 0% |
| — | 基线:手牌用启发式 + 其余判 river | 75.8%(全量) | 0% |
| v4 | 归一化位置 + 相对尺寸 | 88.92% | 71.0% |
| v5 | + 最近邻距离(疏密) | 93.59% | 71.0% |
| v6 | + 同行跨度 | 93.26% | **85.5%** ← 但总分降,牌池 97.2→95.1 |
| v7 | 纯空间聚类,整簇定区域 | 91.90% | **46.8%** ← 对家被串进牌池 |
| **v8(现行)** | v5 + 簇内多数投票 | **94.53%** | 71.0% |
| 对照 | 深度 5 决策树(5 个特征) | 90.77% | — |

### 试过但没成功的特征

* **长宽比 w/h**:对家 2.08、牌池 2.45,AUC 0.32 有区分度,但加进规则后没能稳定提升。
  意外的是最扁的是上家/下家(3.8~3.9),不是对家 —— 侧面两家几乎是贴桌面平视。
* **同行牌数 / 该行跨度**:对家的行跨度最小(0.18,牌池 0.29),v6 用它把对家提到 85.5%,
  但牌池掉 2 个点,总分反而降 0.33。**如果你的目标函数更看重对家,v6 是可取的**。
* **副露=同类连排**:理论上碰杠是 3~4 张相同的牌,但 20 张图里只找到 5 组,样本不足以验证。

### v6 的取舍(值得重新评估)

v6 让对家 71.0% → 85.5%,代价是牌池 97.2% → 95.1%,总分 -0.33。
牌池有 472 个样本、对家只有 62 个,所以总分被牌池主导。

**但下游是打牌模型,它要的是"每家分别打了什么"** —— 牌池本来就是归属不明的公共堆,
判错影响小;对家判错意味着那家的牌河记漏。**按业务价值,v6 可能比 v8 更好。**
这个取舍还没定,可以由你重新评估。

---

## 5. 还没试过的方向

1. **牌面朝向**。对家的牌是倒着朝向我们的,牌面文字方向相反。检测器目前只输出轴对齐框,
   不含朝向;要用这个信号得改检测器(输出旋转框)或对 crop 做朝向判别。
2. **时序**。对家出牌时,新牌从对面方向进入画面;结合麻将逆时针轮转顺序可以反推归属。
   不需要改模型,但需要跨帧信息(现在的区域判定是单帧的)。
   注:上层流水线已经按 track 生命期对区域做多数投票,把单帧 3~11% 的抖动降到 2~8%。
3. **桌面几何**。估计桌子的四个方位再按扇区划分。早期试过一版(v2),因为桌心估计不准而失败,
   但那时没有标注数据可验证,现在有了。
4. **扩标注**。三家的样本只有 157 个(占 17%),而它们恰恰最难。用现成工具再标 20~30 张
   (约 15 分钟)可以让三家样本翻倍。注意:**选图时要避开洗牌/牌局结束的画面**,
   那时牌散在桌上,区域概念不存在。

---

## 6. 约束

* **不要塞进模型**。区域判定必须保持为独立的后处理代码:牌面识别模型是稳定的,桌面几何
  随人/桌/机位而变,把两者绑在一起会让每次机位变化都要重训。而且终点是 AI 眼镜(全新域),
  几何规则重新标 20 张图半小时就能适配,学出来的区域头要重标数千框。
* **可以用小模型**,只要它独立于检测器、输入是几何特征(位置/尺寸/邻居关系)。899 个样本
  现成可用。但注意决策树实测 90.8% 反而不如手写规则的 94.5% —— 20 张图太少,
  人工编码结构比让模型自己学更省数据。
* **保持可解释**。当前是 5 条 if-else,改一个数就能重新验证,不用重训。

---

## 7. 回归测试

`mahjong-rt/tests/test_zones.py`,43 个用例,包含:

* 准确率下限(全量 ≥92%)
* 必须超过"全判 river"基线 15%(v1~v3 手调阈值只有 39.7%,连这条基线都没过,
  但因为当时没有标注数据,谁都没发现)
* 各区域判据的单元用例

改完跑 `python -m pytest tests/ -q` 即可验证没改坏。
